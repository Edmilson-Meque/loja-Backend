spring.profiles.active=prod
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
# ============================================
# CONFIGURA��ES DE UPLOAD DE IMAGENS
# ============================================

# Diret�rio onde as imagens ser�o salvas
app.upload.dir=${user.home}/.ecommerce/uploads

# URL base para acessar as imagens uploadadas
app.upload.base-url=http://localhost:8080/uploads

# Tamanho m�ximo do arquivo (5MB)
app.upload.max-size=5242880

# Extens�es permitidas
app.upload.allowed-extensions=jpg,jpeg,png,gif,webp

# Gerar thumbnails automaticamente
app.image.generate-thumbnails=true

# Dimens�es das thumbnails
app.image.thumbnail.width=150
app.image.thumbnail.height=150

# Dimens�es para imagens redimensionadas
app.image.sizes=small:300x300,medium:600x600,large:1200x1200

# Qualidade das imagens JPEG (0-100)
app.image.quality.jpeg=85
app.image.quality.png=90

# ============================================
# CONFIGURA��ES DE SEGURAN�A PARA UPLOAD
# ============================================

# Validar tipo MIME real do arquivo
app.upload.validate-mime-type=true

# Tipos MIME permitidos
app.upload.allowed-mime-types=image/jpeg,image/png,image/gif,image/webp

# Verificar se � imagem real (l� bytes)
app.upload.validate-real-image=true

# Tamanho m�ximo de largura/altura (0 = ilimitado)
app.image.max-width=4000
app.image.max-height=4000

# ============================================
# CONFIGURA��ES DE PERFORMANCE
# ============================================

# Tamanho do buffer para upload (bytes)
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=20MB

# Habilitar upload ass�ncrono
spring.servlet.multipart.enabled=true